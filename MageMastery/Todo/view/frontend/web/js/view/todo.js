define(['uiComponent'],function() {
    'use strict';
    //console.log("todo");

    return Component.extend({});
    /* we could insert template configuration here instead of in phtml file
    return Component.extend({
        defaults: {

            template: 'MageMastery_Todo/todo'
        }
    });
    */
});
